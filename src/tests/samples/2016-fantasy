2016-fantasy {
  data-store {
    ^ {
      data-store
    }
  }

  presidential {
    < {
      ng-route
      ng-animate
      ng-touch
      data-store
      devbox
      state
      state-graph
      ui-components
    }

    # {
      presidential {
        welcome
        candidates
        league
        find-league
        new-league {
          construct
          draft
          selection {
            pasture
            set-draft
            stable
          }
          sidebar {
            players {
              invite
            }
          }
        }
      }
    }

    > {
      '/' : welcome
      '/candidates': candidates
    }
  }

  state {
    ^ {state}
  }

  state-graph {
    # {stage}
  }

  ui-components {
    directives {
      delayed-src
      scroll
      scrollControl
      scrollSync
      scrollSwap
      sensitiveTouch
    }
  }
}